<h1>Buscar palavras chave:</h1>
<form id="buscar_palavraschave" action="/buscar-palavras-chave" method="GET">

  <div class="input-group mb-3">
    <input id="formbuscachave" type="text" class="form-control" aria-label="Default" name="chave"
      placeholder="Buscar palavras chave" aria-describedby="inputGroup-sizing-default">
  </div>
  <button class="btn btn-secondary" type="submit">Buscar</button>
</form>
<br>
<h4> Palavras chave {{resultado}}</h4>

{{#each chaves}}
{{this}} - 
{{/each}}